export const soma = (v1, v2) => v1 + v2;
export const subtracao = (v1, v2) => v1 - v2;
export const multiplicacao = (v1, v2) => v1 * v2;
export const divisao = (v1, v2) => (v2 !== 0 ? v1 / v2 : 'Erro');
export const porcentagem = (v1, v2) => (v1 * v2) / 100; 
